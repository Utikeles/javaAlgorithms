public static boolean hasValue(int[][] arr, int value, int row)
{
    if (arr.length<row) {
        return false;
    }
    for (int i = 0 ; i < arr.length; i++) {
        if (arr[i][row]==value) {
            return true;
        }
    }
    return false;
}