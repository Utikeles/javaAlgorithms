public static boolean isSquare(int[][] arr)
{
    int standard = arr[0].length;
    for (int i = 0;i<arr.length;i++) {
            if (arr[i].length != standard || arr.length != standard) {
                return false;
            }
        }
    return true;
}