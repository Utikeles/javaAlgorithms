public boolean sumThree(ArrayList<Integer> arr)
{
    int count=0; // count of # of int 3's
    for (int i = 0;i < arr.size();i++) {
        if (arr.get(i)==3) {
            count++;
        }
    }
    return(count>=3);
}